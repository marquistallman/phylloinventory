# Dependencias del cliente CLI (thin client).
# La mayoria de la logica ahora vive en los microservicios Docker.

# HTTP + WebSocket
httpx>=0.25
websockets>=12

# Audio del microfono (opcional — la CLI funciona sin voz)
sounddevice>=0.4

# UI
rich>=13.0

# Importador de catalogo Excel (local, no en Docker)
openpyxl>=3.1
psycopg2-binary>=2.9
